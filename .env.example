# ==================================
# 🚀 운명의 해답 (SajuFortune) 환경 설정
# ==================================
#
# ⚠️ 보안 주의사항:
# 1. 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env
# 2. .env 파일은 절대 Git에 커밋하지 마세요 (.gitignore에 포함됨)
# 3. 프로덕션 환경에서는 모든 비밀키를 강력한 랜덤 값으로 변경하세요
# 4. Kubernetes/Docker 시크릿을 사용하는 경우 환경변수로 주입하세요
# ==================================

# ----------------------------------
# 🔴 필수 설정 (REQUIRED)
# ----------------------------------

# 데이터베이스 연결 (PostgreSQL 16+)
# 형식: postgresql://username:password@host:port/database
# 예시 (NeonDB): postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/saju_fortune?sslmode=require
# 예시 (로컬): postgresql://postgres:password@localhost:5432/saju_fortune
DATABASE_URL=postgresql://username:password@localhost:5432/saju_fortune

# 세션 암호화 키 (최소 64자 권장, 영문+숫자+특수문자 조합)
# ⚠️ CRITICAL: 프로덕션에서는 반드시 강력한 랜덤 키로 변경하세요!
# 생성 방법: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 예시 길이: 7f3d8e9a2b4c6d1e0f9a8b7c6d5e4f3a2b1c0d9e8f7a6b5c4d3e2f1a0b9c8d7e6f5a4b3c2d1e0f...
SESSION_SECRET=your-super-secret-session-key-change-this-in-production-minimum-64-characters

# ----------------------------------
# 서버 설정 (SERVER)
# ----------------------------------

# 환경 설정 (development | production)
NODE_ENV=development

# 서버 포트 (기본값: 5000)
PORT=5000

# 프론트엔드 URL (프로덕션 CORS 설정용)
# 개발: http://localhost:5000
# 프로덕션: https://sajufortune.com
FRONTEND_URL=https://yourdomain.com

# ----------------------------------
# 🛡️ 보안 설정 (SECURITY)
# ----------------------------------

# Rate Limiting 설정
# 사주 계산: 10 requests / 15분 (하드코딩됨)
# 후원: 5 requests / 15분 (하드코딩됨)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS 허용 도메인 (쉼표로 구분)
# CORS_ORIGINS=https://sajufortune.com,https://www.sajufortune.com

# ----------------------------------
# 💾 캐싱 설정 (CACHING)
# ----------------------------------

# Redis 캐싱 (프로덕션 필수, 성능 향상)
# 개발: NodeCache (In-Memory) 자동 사용
# 프로덕션: Redis 설정 권장
# 예시 (Upstash): rediss://default:password@upstash-redis.upstash.io:6379
# REDIS_URL=redis://localhost:6379

# 캐시 TTL (초 단위)
# CACHE_TTL=7200

# ----------------------------------
# 💳 결제 설정 (PAYMENT) - 후원 기능
# ----------------------------------

# Stripe 결제 게이트웨이
# 개발: sk_test_... 사용
# 프로덕션: sk_live_... 사용 (⚠️ CRITICAL)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ----------------------------------
# 📧 이메일 설정 (EMAIL) - 선택사항
# ----------------------------------

# 이메일 서비스 선택 (sendgrid | ses | resend | smtp)
# EMAIL_SERVICE=sendgrid

# SendGrid 설정
# SENDGRID_API_KEY=SG.your_sendgrid_api_key

# AWS SES 설정
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_SES_FROM_EMAIL=noreply@sajufortune.com

# Resend 설정
# RESEND_API_KEY=re_your_resend_api_key

# SMTP 서버 설정 (Gmail, Office365 등)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM_EMAIL=noreply@sajufortune.com
# SMTP_FROM_NAME=운명의 해답

# ----------------------------------
# 📊 분석 및 모니터링 (ANALYTICS & MONITORING)
# ----------------------------------

# Google Analytics 4 (프로덕션 권장)
# GA 계정에서 측정 ID 발급: https://analytics.google.com
# 형식: G-XXXXXXXXXX
# VITE_GA_MEASUREMENT_ID=G-YOUR_MEASUREMENT_ID

# Sentry 에러 추적 (프로덕션 권장)
# SENTRY_DSN=https://your-key@sentry.io/your-project-id
# SENTRY_ENVIRONMENT=production
# SENTRY_SAMPLE_RATE=1.0

# Application Insights (Azure)
# APPINSIGHTS_INSTRUMENTATIONKEY=your_instrumentation_key

# 로그 레벨 (debug | info | warn | error)
LOG_LEVEL=info
# LOG_FILE_PATH=./logs/app.log

# ----------------------------------
# 🚀 배포 설정 (DEPLOYMENT) - 프로덕션 전용
# ----------------------------------

# CDN URL (정적 파일 제공)
# CDN_URL=https://cdn.sajufortune.com

# 도메인 설정
# DOMAIN=sajufortune.com
# SSL_CERT_PATH=/etc/letsencrypt/live/sajufortune.com/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/sajufortune.com/privkey.pem

# ----------------------------------
# 🧪 개발/테스트 설정 (DEVELOPMENT/TEST)
# ----------------------------------

# 테스트 환경 설정
# TEST_DATABASE_URL=postgresql://postgres:password@localhost:5432/saju_fortune_test
# TEST_REDIS_URL=redis://localhost:6380

# 디버그 모드
# DEBUG=true
# VERBOSE_LOGGING=true

# ==================================
# 설정 완료 후 확인 사항:
# ==================================
#
# 1. DATABASE_URL 연결 테스트:
#    psql $DATABASE_URL -c "SELECT version();"
#
# 2. SESSION_SECRET 길이 확인:
#    echo $SESSION_SECRET | wc -c  # 64자 이상 권장
#
# 3. Redis 연결 테스트 (프로덕션):
#    redis-cli -u $REDIS_URL ping
#
# 4. Stripe 키 검증:
#    curl https://api.stripe.com/v1/charges -u $STRIPE_SECRET_KEY:
#
# 5. 환경변수 로드 확인:
#    npm run dev  # 개발 서버 시작 후 로그 확인
#
# ==================================
