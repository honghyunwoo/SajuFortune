# ==================================
# 🚀 사주풀이 서비스 환경 설정
# ==================================

# ----------------------------------
# 필수 설정 (REQUIRED)
# ----------------------------------

# 데이터베이스 연결 (PostgreSQL 16+)
# 형식: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:5432/saju_fortune

# 세션 암호화 키 (최소 32자 이상, 영문+숫자+특수문자 조합)
# ⚠️ 운영 환경에서는 반드시 강력한 랜덤 키로 변경하세요!
# 생성 방법: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# ----------------------------------
# 서버 설정 (SERVER)
# ----------------------------------

# 환경 설정 (development | production)
NODE_ENV=development

# 서버 포트 (기본값: 5000)
PORT=5000

# 프론트엔드 URL (프로덕션 CORS 설정용)
FRONTEND_URL=https://yourdomain.com

# ----------------------------------
# 보안 설정 (SECURITY)
# ----------------------------------

# Rate Limiting 설정 (밀리초)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ----------------------------------
# 캐싱 설정 (CACHING) - 선택사항
# ----------------------------------

# Redis 캐싱 (성능 향상을 위해 프로덕션에서 권장)
# REDIS_URL=redis://localhost:6379

# ----------------------------------
# 결제 설정 (PAYMENT) - 선택사항
# ----------------------------------

# Stripe 결제 게이트웨이
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ----------------------------------
# 모니터링 설정 (MONITORING) - 선택사항
# ----------------------------------

# Sentry 에러 추적 (프로덕션 권장)
# SENTRY_DSN=your_sentry_dsn_for_error_tracking

# 로그 레벨 (debug | info | warn | error)
LOG_LEVEL=info
LOG_FILE_PATH=./logs/app.log

# ----------------------------------
# 이메일 설정 (EMAIL) - 선택사항
# ----------------------------------

# SMTP 서버 설정 (고객 지원용)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
